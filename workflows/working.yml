name: Working Scraper
  on:
    workflow_dispatch:
  jobs:
    test:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - run: echo "Hello World"
        - run: ls -la
        - run: pwd
